# ===========================================
# Event Ticketing Platform - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# ===========================================

# ----- General -----
NODE_ENV=development

# ----- MongoDB -----
# Option 1: Use MongoDB Atlas (recommended for production)
# Get your connection string from MongoDB Atlas: https://cloud.mongodb.com
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net

# Option 2: Use local MongoDB (uncomment the mongodb service in docker-compose.yml)
# MONGODB_URI=mongodb://mongodb:27017

# Legacy credentials (not needed for Atlas or docker-compose setup)
# MONGO_ROOT_USERNAME=admin
# MONGO_ROOT_PASSWORD=your_secure_password_here

# ----- Redis -----
# Redis configuration (uses Docker service name internally)
REDIS_HOST=redis
REDIS_PORT=6379

# ----- JWT Authentication -----
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random
JWT_EXPIRES_IN=7d

# ----- CORS -----
# Frontend origin for CORS (use http://localhost for local dev with Docker)
CORS_ORIGIN=http://localhost

# ----- Frontend URL -----
# Used for email links, password reset, etc.
FRONTEND_URL=http://localhost

# ----- Google OAuth -----
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# ----- Google Maps API -----
# For venue location features
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# ----- Stripe Payment -----
# Get these from Stripe Dashboard: https://dashboard.stripe.com/
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# ----- Cloudinary -----
# For image uploads: https://cloudinary.com/
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ----- Gemini API -----
# For AI chatbot features: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key

# ----- Email (SMTP) -----
# For sending emails (verification, tickets, etc.)
# Using Mailhog for local development (no auth needed)
# For production, use real SMTP like Gmail (for Gmail, SMTP_HOST=smtp.gmail.com, SMTP_PORT=587)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=your_email_address

# ----- Vite Frontend Variables -----
# These are passed to the frontend build
# Leave empty to use Nginx proxy (recommended)
VITE_API_URL=
